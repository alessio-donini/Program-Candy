@page
@model Candy.Pages.AmministrazioneModel
@{
}
<div class="text-center adm-page">
    <div class="container">
        @for (int i = 0; i < @Program.products.Count; i++)
        {
            <div class="row">
                <div class="col-10 offset-1 box-container">
                    <div class="row element">
                        <div class="col-4 box-nome">
                            <h6 class="text">@Program.products[i].nome</h6>
                        </div>
                        <div class="col-4 box-quantità">
                            <h6 class="text">@Program.products[i].quantita</h6>
                        </div>
                        <div class="col-4 box-restock">
                            <h6 class="text">restock</h6>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>>
    <span>
        <form asp-page="/Index" asp-page-handler="Exit" class="form-signout" method="post">
            <button class="btn btn-primary btn1" type="submit" onclick="Exit">Esci</button>
        </form>
    </span>
</div>