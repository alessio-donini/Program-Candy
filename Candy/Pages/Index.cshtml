@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
<div class="text-center">
    @{ Program.UpdateList(); }
    <div class="row">
        @{
            for (int i = 0; i < Program.products.Count; i++)
            {
                /*<div class="col-12 col-md-6 col-lg-4 caramella">
                    <a asp-page="/Prodotto" asp-route-ID=@Program.products[i].nome>
                    <img class="img-fluid caramellaimg" src="~/images/@Program.p.FormatCandy(Program.products[i].nome)" />
                    @{
                        Disp(@Program.products[i]);
                    }
                    </a>
                </div>*/
                <div class="col-4">
                    <div class="box">
                        <a asp-page="/Prodotto" asp-route-ID=@Program.products[i].nome>
                            <div class="img-box">
                                <img class="img-fluid candy-img" src="~/images/@Program.p.FormatCandy(Program.products[i].nome)" />
                            </div>
                            <div class="detail-box">
                                <h6>
                                    @{
                                        Disp(@Program.products[i]);
                                    }
                                </h6>
                            </div>
                        </a>
                    </div>
                </div>
            }
        }
    </div>
</div>

@{
    void Disp(Prodotto prod)
    {
        if (prod.quantita > 0)
        {
            <p>@prod.nome<br/>
               @prod.prezzo€
            </p>
        }
        else
        {
            <p style="color: red;">Esaurito</p>
        }
    }
}